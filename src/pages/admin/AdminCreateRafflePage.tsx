export default function AdminCreateRafflePage() {
  return (
    <div style={{ padding: 40 }}>
      <h1>CREATE PAGE TEST</h1>
      <p>If you can see this, the route is fine and the crash is inside the real form.</p>
    </div>
  );
}
